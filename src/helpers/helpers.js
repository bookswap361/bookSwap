var HbsHelpers = {};

HbsHelpers.displayResolveButton = function(userId, ownerId, domId) {
    // if userId == ownerId
    // create button and append
};

HbsHelpers.formatThreadLink = function(threadId) {
    return '/forum/?id=' + threadId;
};

module.exports = HbsHelpers;
