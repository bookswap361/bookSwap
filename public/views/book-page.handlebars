<div class="container">
    <div class="row">
        <div class="col-md-3">
            <img src="{{thumbnail_url}}">
        </div>
        <div class="col-md-9 mx-auto">
            <h3>{{title}}</h3> by {{name}}
            <hr>
            {{description}}
            <P>Genres: {{genres}}</P>
        </div>
    </div>

    <div class="row">
        <button type="button" class="btn btn-primary" onclick="showAddForm()">Add this book to your collection of owned books:</button>
        <button type="button" class="btn btn-danger" onclick="window.location.href='../search';">Search Again</button>
    </div>

    <div class="row">
        <div id="chooseCondition" class="container hidden">
            <div id="addForm">

                <label for="condition"><a href="#" target="_blank">Choose the condition of your book to trade:</a></label>
                <select name="condition" id="newCondition">
                    <option value="excellent">Excellent condition: 15pts</option>
                    <option value="good">Good condition: 10pts</option>
                    <option value="acceptable">Acceptable condition: 5pts</option>
                </select> 

                <input id="newOl_key" value= "{{ol_key}}" hidden>
                <input id="newDescription" value= "{{description}}" hidden>
                <input id="newThumbnail_url" value= "{{thumbnail_url}}" hidden>
                <input id="newTitle" value="{{title}}" hidden> 
                <input id="newUser" value= "{{user}}" hidden> 
                <input id="newAuthor" value= "{{author}}" hidden> 

                <button type="button" class="btn btn-outline-primary btn-large" id="addBookBtn" onclick="addBook()">Add Book!</button>

                <P><div id="confirm"></div></P>
            </div>
        </div>
    </div>

    
    <div class="row">
        <div class="col-md-8 mx-auto">
        Open Swaps for this Book: {{copies}}
        </div>
    </div>
</div>

{{!-- <div class="hidden">

</div>

<div class="hidden">
    <input id="title" value=`{{title}}`>
    <input id="list_id" value=`{{title}}`>
    <input id="traded_by" value=`{{user}}`>
    <input id="title" value=`{{title}}`> 
</div> --}}

<script src="../scripts/bookpage.js"></script>